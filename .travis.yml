language: node_js

services:
  - docker

before_install:
  - docker build -t huberthao/kb:lst . 
  - docker container run --name kb -d
  - echo "$DH_PASSWD" | docker login -u "$DH_USER" --password-stdin
  - docker push "$DH_USER"/kb:lst
